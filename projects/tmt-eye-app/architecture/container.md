# Container Diagram — tmt-eye-app

> **C4 Level 2** — the system broken down into deployable/runnable containers. Audience: the dev team. One diagram per managed project; usually zoomed in from the L1 context diagram.

## Diagram

```mermaid
C4Container
    title Container Diagram for tmt-eye-app

    Person(user, "Fleet Operator / Driver / Admin", "Uses the dispatch UI in the browser")

    System_Boundary(boundary, "tmt-eye-app") {
        Container(web, "Web App + API", "Next.js 16 (App Router)", "Renders UI (i18n via app/[lng]), exposes API route proxies, handles session via NextAuth v5 (beta)")
    }

    System_Ext(backend, "tmt Backend API", "External REST + WebSocket service", "Business logic and persistence — dispatch jobs, vehicles, drivers, maintenance, reports")
    System_Ext(realtime, "Socket.IO Server", "Realtime channel", "Live vehicle tracking and event push — may be same host as backend API")
    System_Ext(maps, "Google Maps Platform", "Maps JS API + Places Autocomplete")
    System_Ext(firebase, "Firebase Cloud Messaging", "Web push notifications + Firebase Admin on server side")
    System_Ext(postmark, "Postmark", "Transactional email")
    System_Ext(analytics, "Vercel Analytics", "Web vitals + page view metrics")

    Rel(user, web, "Uses", "HTTPS")
    Rel(web, backend, "Calls for data", "HTTPS / JSON (axios)")
    Rel(web, realtime, "Subscribes to live events", "WebSocket")
    Rel(web, maps, "Loads maps + autocomplete", "HTTPS")
    Rel(web, firebase, "Requests + receives push tokens", "HTTPS")
    Rel(web, postmark, "Sends transactional mail", "HTTPS API")
    Rel(web, analytics, "Emits pageview + vitals", "HTTPS beacon")
```

> **Note**: this diagram was auto-generated by `/handover` on 2026-04-19 from repo signals (`package.json`, `Dockerfile`, folder structure under `app/`, `components/`, `data/queries/`). It is a **starting point** — review and refine.
>
> - Container labels and tech strings — the detector may have picked a framework version wrong
> - Inferred relationships — `user → web` assumes HTTPS; adjust if your stack uses something else
> - External systems — anything your team uses that isn't in `package.json` (e.g. infra-only dependencies, direct cloud APIs called via HTTP) won't have been detected. The **backend API** is inferred from `@auth/prisma-adapter` + `axios` + the absence of a local `schema.prisma` — please confirm the real topology (one backend? multiple? is Socket.IO the same host?).
>
> Update the "Maintenance" section below once the diagram is stable.

## Maintenance

(From the template — update when L2 containers change.)
